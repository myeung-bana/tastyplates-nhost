table:
  name: restaurant_locations
  schema: public

select_permissions:
  - role: public
    permission:
      columns: "*"
      filter:
        is_active: { _eq: true }
      allow_aggregations: true

  - role: admin
    permission:
      columns: "*"
      filter: {}
      allow_aggregations: true
