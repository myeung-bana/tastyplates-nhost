# Copy to config.yaml at repo root (committed). Nhost deploy reads this for migrations.
# Metadata: manage in Hasura Console unless you commit a full export under nhost/metadata/.
version: 3
migrations_directory: nhost/migrations
